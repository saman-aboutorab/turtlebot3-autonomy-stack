# ROS2 build and install artifacts
build/
install/
log/

# Map files are tracked — they are the output of real robot SLAM sessions
# and are needed for Nav2 in Step 10. Small files (~40 KB each).

# Python
__pycache__/
*.pyc
*.pyo

# Editor
.vscode/
.idea/
